<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
	<!-- Allows HTTP (cleartext). Remove or tighten for production if fully HTTPS. -->
	<base-config cleartextTrafficPermitted="true" />
</network-security-config>
